{
  "name": "anotador",
  "version": "1.0.0",
  "license": "MIT",
  "dependencies": {
    "gh-pages": "^3.2.3",
    "parcel": "^2.0.0-rc.0"
  },
  "scripts": {
    "dev": "parcel serve ./src/index.html --port 4321",
    "build": "parcel build ./src/index.html --dist-dir build --public-url /anotador-app",
    "deploy": "gh-pages -d build"
  }
}
